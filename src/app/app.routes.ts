import { Routes } from '@angular/router';
import { QuoteCreatorComponent } from './quote-creator/quote-creator';

export const routes: Routes = [
    { path: '', component: QuoteCreatorComponent }
];
